# Bug?????¦Á??? (2026-06-03)

## ???????

???????????????????§Ù???? **2??P1/P2????Bug**??????????????????????

| ????? | ???? | ?? | ??????? |
|--------|------|------|---------|
| P1 | GC head==tail????????? | ? ????? | ???§Ó?????? |
| P2 | write_back????write_through????? | ? ????? | ? ?????????? |

---

## ???????

### 1. P1: GC head==tail?????????

**???**: `eflash_ftl/eflash_ftl.c:2593-2617`

**????**: GC????????????`head==tail`???????§¹????????????????????????????????Radix Tree???????????????

**????** (? ??????):
```c
if (FTL->gc_head_page == FTL->gc_tail_page) {
    FTL->gc_head_page++;  // ?????????
    if (FTL->gc_head_page == EFLASH_TOTAL_PAGES) {
        FTL->gc_head_page = 0;
    }
    return 0;  // ? ????????????Ÿ??????
}
```

**?????** (? ??????):
```c
if (FTL->gc_head_page == FTL->gc_tail_page) {
    FTL_DEBUG("[GC_COLLECT] Page %d is VALID (head==tail=%d), migrating before pointer move...\n", 
             ppn, FTL->gc_head_page);
    
    // ? ???????§¹???
    if (gc_migrate_page(ppn) != 0) {
        FTL_DEBUG("[GC_COLLECT] ERROR: Migration failed for page %d (head==tail)\n", ppn);
        return -1;
    }
    
    // ?????????
    FTL->gc_head_page++;
    if (FTL->gc_head_page == EFLASH_TOTAL_PAGES) {
        FTL->gc_head_page = 0;
    }
    
    FTL_DEBUG("[GC_COLLECT] Page %d migrated, head moved to %d\n", 
             ppn, FTL->gc_head_page);
    return 0;
}
```

**???**:
- ? ??????????????
- ? ????Radix Tree?????
- ? ?????????????????

---

### 2. P2: write_back????write_through?????

**???**: `eflash_ftl/eflash_ftl.c:1833-1836`

**????**: ??????????`write_through`¡¤?????????????????0?????¡Â????????§Ô????

**????** (? ????????):
```c
slot = content_cache_find_min_seq();
if (slot < 0) {
    eflash_ftl_write_through(sector_id, data);
    return 0;  // ? ????write_through????????0
}
```

**?????** (? ??????????):
```c
slot = content_cache_find_min_seq();
if (slot < 0) {
    // All cache slots invalid, fallback to write-through
    return eflash_ftl_write_through(sector_id, data);  // ? ??????????
}
```

**???**:
- ? ???¡Â?????????§Õ?????
- ? ????????????????
- ? ?????????

---

### 3. ????????????: test_write_back_cache_exhausted

**???**: `eflash_ftl/eflash_ftl_tests.c`

**???????**:
1. ????????: §Õ??4?????sector????????cache??
2. ????????: §Õ???5??sector????LRU????
3. ?????????: ???`write_back`????`write_through`?????????????????0??
4. ??????????: ?????§Õ??sector?????????????
5. ???????????: ??????????sector?????????flush??Flash

**???????**:
```c
static int test_write_back_cache_exhausted(void) {
    TEST(write_back_cache_exhausted);

    init_test_flash();
    eflash_ftl_init();

    uint8_t write_buf[USER_DATA_SIZE];
    uint8_t read_buf[USER_DATA_SIZE];

    // Fill all 4 cache slots with different sectors
    for (int i = 0; i < 4; i++) {
        memset(write_buf, (uint8_t)(0x10 + i), USER_DATA_SIZE);
        ASSERT(eflash_ftl_write_back((uint16_t)(100 + i), write_buf) == 0, 
               "write_back to fill cache slot");
    }

    // Now write to 5th sector - should trigger eviction of LRU slot
    memset(write_buf, 0xCD, USER_DATA_SIZE);
    int ret = eflash_ftl_write_back(200, write_buf);
    
    // Key assertion: should return write_through result, not hardcoded 0
    ASSERT(ret == 0, "write_back should return write_through result on cache exhaustion");

    // Verify data was written to flash
    ASSERT(eflash_ftl_read(200, read_buf) == 0, "read sector 200");
    ASSERT(memcmp(read_buf, write_buf, USER_DATA_SIZE) == 0, "data should match");

    // Verify evicted slot data was flushed (sector 100 should be readable)
    memset(read_buf, 0, USER_DATA_SIZE);
    memset(write_buf, 0x10, USER_DATA_SIZE);
    ASSERT(eflash_ftl_read(100, read_buf) == 0, "read evicted sector 100");
    ASSERT(memcmp(read_buf, write_buf, USER_DATA_SIZE) == 0, "evicted data should be flushed");

    cleanup_test_flash();
    PASS();
}
```

---

### 4. ????????????

**??????**:
- `eflash_ftl/eflash_ftl.c:73` - `FTL_DEBUG_ENABLE` ???????0
- `eflash_ftl/eflash_mgr.c:10-20` - `FTL_DEBUG` ????`FTL_DEBUG_ENABLE`????
- `Makefile` - ????`CFLAGS_DEBUG`??`test-debug-*`???

**??¡Â???**:
```bash
# ??????????????????
make test-all

# ??????????????????????
make test-debug-all
```

---

## ???????

| ??????? | ?????? | ?? |
|---------|--------|------|
| ???????? | **26** (?25) | ? ?????? |
| ?????????? | 19 | ? ?????? |
| ??????? | 27 | ? ?????? |
| ???????? | 1 | ? ??? |
| **???** | **73** | ? **100%???** |

---

## ??????????

### write_back????????

?????????§Ö???`eflash_ftl_write_back`??????

| ??? | ??????? | ????????? | ?? |
|------|---------|-----------|------|
| `eflash_ftl.c` | 1 (????) | - | ? ???????? |
| `eflash_ftl_tests.c` | 2 | `ASSERT(ret == 0, ...)` | ? ?????? |
| `eflash_ftl_tests_code_region.c` | 23 | `assert(ret == 0 && ...)` | ? ?????? |

**????**: ???§Ö??¡Â????????????????????????????

---

## ??????????

| ??? | ???? |
|------|------|
| ??????? | ? ????????? |
| ??????? | ? ????????? |
| ?????? | ? ?????? |
| ??????? | ? ????1???????? |
| ???????? | ? ??????????????? |

---

## ????????

1. **???????**: ???????????§Þ??GC head==tail????????????
2. **???????**: ????????????¼T?????????Flash???write_through????
3. **???????**: ?????????????????????????????

---

*???????????: 2026-06-03*
*??????: Round 1*
*??????: AI Assistant*
